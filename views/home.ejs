<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
        font-family: "Poppins", sans-serif;
      }

      .navbar {
        background-color: #f2f2f2 !important;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
      }
      .navbar-brand {
        font-weight: 700;
        font-size: 1.5rem;
        color: #333 !important;
        letter-spacing: 0.5px;
      }
      .btn-logout {
        font-weight: 600;
        padding: 8px 15px;
        border-radius: 8px;
        transition: all 0.3s ease-in-out;
        font-size: 14px;
      }
      .btn-logout:hover {
        background-color: #c82333;
        color: #fff;
      }

      .main-container {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 20px;
        align-items: start;
      }

      .table-container {
        padding: 20px;
        width: 100%;
      }
      .table {
        text-align: center;
        border-radius: 10px;
        overflow: hidden;
      }
      .table thead {
        background-color: #343a40;
        color: white;
        font-weight: 600;
      }
      .table tbody tr:hover {
        background-color: #f8f9fa;
        transition: all 0.3s ease-in-out;
      }
      .table th,
      .table td {
        padding: 12px;
        font-size: 14px;
      }

      .table-responsive {
        overflow-x: auto;
        width: 100%;
      }

      @media (max-width: 768px) {
        .table-container {
          width: 100%;
          overflow-x: auto;
        }
        .table th,
        .table td {
          font-size: 12px; /* Perkecil ukuran font di layar kecil */
          white-space: nowrap; /* Agar teks tetap dalam satu baris */
        }
        .table {
          border-radius: 0 !important; /* Hapus efek melengkung */
          overflow: hidden; /* Pastikan border tidak terpengaruh */
        }

        .table thead {
          border-top-left-radius: 0 !important;
          border-top-right-radius: 0 !important;
        }

        .table tbody {
          border-bottom-left-radius: 0 !important;
          border-bottom-right-radius: 0 !important;
        }
      }

      .status-container {
        display: flex;
        flex-direction: column;
        gap: 20px;
        align-items: center;
      }
      .card {
        width: 100%;
        max-width: 350px;
        border-radius: 12px;
        box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease-in-out;
        border: none;
        background: linear-gradient(145deg, #ffffff, #f1f3f5);
      }
      .card:hover {
        transform: translateY(-5px);
        box-shadow: 0px 12px 20px rgba(0, 0, 0, 0.15);
      }
      .card-title {
        font-weight: 600;
        font-size: 1.2rem;
        color: #333;
      }
      .status-icon {
        width: 35px;
        height: 35px;
        border-radius: 50%;
        display: inline-block;
        transition: all 0.3s ease-in-out;
      }
      .bg-success {
        background-color: #28a745 !important;
        box-shadow: 0px 0px 10px rgba(40, 167, 69, 0.7);
      }
      .bg-danger {
        background-color: #dc3545 !important;
        box-shadow: 0px 0px 10px rgba(220, 53, 69, 0.7);
      }

      @keyframes blink {
        0%,
        100% {
          opacity: 1;
        }
        50% {
          opacity: 0.5;
        }
      }
      .alarm-active {
        color: red;
        font-weight: bold;
        animation: blink 1s infinite;
        font-size: 1.1rem;
      }

      @media (max-width: 768px) {
        .main-container {
          display: flex;
          flex-direction: column;
          align-items: center;
        }
        .table-container {
          width: 100%;
        }
        .table {
          font-size: 13px;
        }
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg">
      <div class="container">
        <a class="navbar-brand" href="#">ESP32 Status</a>
        <button class="btn btn-danger btn-logout" id="logoutBtn">Logout</button>
      </div>
    </nav>

    <% const alarmUsers = (typeof activeUserAlarms !== "undefined" ?
    activeUserAlarms : []).map(u => ({ number: u, type: "Alarm" })); const
    brankasUsers = (typeof activeUserBrankas !== "undefined" ? activeUserBrankas
    : []).map(u => ({ number: u, type: "Brankas" })); const activeUsers =
    alarmUsers.concat(brankasUsers); %>

    <div class="container my-5 main-container">
      <div class="table-container">
        <h5 class="mb-3" style="font-weight: 600">📲 User WhatsApp Aktif</h5>
        <div class="table-responsive">
          <table class="table table-sm table-bordered">
            <thead class="table-dark">
              <tr>
                <th style="width: 50px">No</th>
                <th>Nomor WhatsApp</th>
                <th>Tipe User</th>
              </tr>
            </thead>
            <tbody>
              <% if (activeUsers.length === 0) { %>
              <tr>
                <td colspan="3" class="text-muted">Tidak ada user aktif</td>
              </tr>
              <% } else { %> <% activeUsers.forEach((user, index) => { %>
              <tr>
                <td><%= index + 1 %></td>
                <td style="font-size: 0.9rem"><%= user.number %></td>
                <td style="font-size: 0.9rem"><%= user.type %></td>
              </tr>
              <% }) %> <% } %>
            </tbody>
          </table>
        </div>
      </div>

      <div class="status-container">
        <% esp32Devices.forEach((device) => { %>
        <div class="card p-4 text-center">
          <div class="card-body">
            <h5 class="card-title"><%= device.name %></h5>
            <div
              class="status-icon <%= device.status === 'Connected' ? 'bg-success' : 'bg-danger' %>"
            ></div>
            <p class="card-text mt-2">
              Status: <strong><%= device.status %></strong>
            </p>

            <% if (device.name === "ESP32-001") { %>
            <p class="mt-3 fs-5">
              🚨 Alarm:
              <span
                class="<%= device.alarmStatus === 'Active' ? 'alarm-active' : 'text-muted' %>"
              >
                <%= device.alarmStatus %>
              </span>
            </p>
            <% } %>
          </div>
        </div>
        <% }) %>
      </div>
    </div>

    <script>
      document.getElementById("logoutBtn").addEventListener("click", () => {
        window.location.href = "/auth/logout";
      });

      setInterval(() => {
        window.location.reload();
      }, 5000);
    </script>
  </body>
</html>
